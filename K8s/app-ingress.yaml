apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: backend-ingress
  namespace: default  
  annotations:
    # Ma t7ottouch 'ssl-passthrough' houni, 5ater Spring Boot ye5dem HTTP
spec:
  ingressClassName: nginx
  rules:
  - host: api.4.251.133.114.nip.io  
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: backend-service
            port:
              number: 8080
